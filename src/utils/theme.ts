import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fefefe",
      light: "#f5f5f5",
      dark: "#00133b",
    },
    error: {
      main: "#e02020",
    },
  },
});

export default theme;
